# GitHub OAuth App Settings
# Create a GitHub OAuth App at: https://github.com/settings/developers
# Leave empty if using Personal Access Token (PAT) authentication only

# OAuth Client ID (optional - for OAuth flow)
VITE_GITHUB_CLIENT_ID=

# OAuth Redirect URI (optional - for OAuth flow)
# Development: http://localhost:5173/callback
# Production: https://your-app.vercel.app/callback
VITE_GITHUB_REDIRECT_URI=http://localhost:5173/callback

# Note: Personal Access Token (PAT) will be stored in browser LocalStorage
# Users can generate PAT at: https://github.com/settings/tokens
# Required scope: 'repo' (Full control of private repositories)
